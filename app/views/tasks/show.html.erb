<h1>U SucK AnyWAy</h1>

<p><%= @task.details %></p>
  <%=link_to "Return to the tasks list", tasks_path%>
  <%=link_to "Update task", tasks_path(@task)%>
  <%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
